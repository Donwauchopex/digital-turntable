// Arm angle constants
export const ARM_ANGLE_REST = -90; // Fully parked, off the record
export const ARM_ANGLE_NO_INTERSECT = -76.3; // Clear of platter (safe to flip if angle <= this)
export const ARM_LEAD_IN_START_ANGLE = -71; // Start of lead-in area (where music starts)
export const ARM_START_ANGLE = -69; // Start of playable music (outer groove)
export const ARM_END_ANGLE = -45; // End of playable music (inner groove / run-out)
